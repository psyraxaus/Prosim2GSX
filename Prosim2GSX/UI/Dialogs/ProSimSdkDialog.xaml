<Window x:Class="Prosim2GSX.UI.Dialogs.ProSimSdkDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="ProSim SDK Configuration" 
        Height="262" 
        Width="600"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        WindowStyle="ToolWindow">
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Title and description -->
        <TextBlock Grid.Row="0" 
                   Text="ProSim SDK Path Configuration" 
                   FontSize="16" 
                   FontWeight="Bold" 
                   Margin="0,0,0,10"/>
        
        <TextBlock Grid.Row="1" 
                   TextWrapping="Wrap"
                   Margin="0,0,0,15">
            <Run Text="The ProSim SDK file (ProSimSDK.dll) is required for the Prosim interface to work properly."/>
            <LineBreak/>
            <Run Text="Please browse and select the ProSimSDK.dll file location."/>
        </TextBlock>

        <!-- File path input -->
        <Label Grid.Row="2" 
               Content="SDK Path:" 
               Margin="0,0,0,5"/>
        
        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <TextBox Grid.Column="0" 
                     Name="PathTextBox" 
                     VerticalAlignment="Center"
                     Padding="5"
                     IsReadOnly="True"/>
            
            <Button Grid.Column="1" 
                    Name="BrowseButton" 
                    Content="Browse..." 
                    Width="80" 
                    Margin="10,0,0,0"
                    Padding="5"
                    Click="BrowseButton_Click"/>
        </Grid>

        <!-- Status message -->
        <TextBlock Grid.Row="4" 
                   Name="StatusText"
                   Foreground="Red"
                   TextWrapping="Wrap"
                   Margin="0,10,0,0"
                   Visibility="Collapsed"/>

        <!-- Buttons -->
        <StackPanel Grid.Row="5" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right"
                    Margin="0,20,0,0">
            <Button Name="OkButton" 
                    Content="OK" 
                    Width="80" 
                    Margin="0,0,10,0"
                    IsEnabled="False"
                    Click="OkButton_Click"/>
            <Button Name="CancelButton" 
                    Content="Cancel" 
                    Width="80"
                    Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>